{
  "name": "learnex-ai",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('LEARNEX AI build OK — no src folder required.')\"",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/genai": "^1.15.0",
    "express": "^5.1.0"
  }
}
